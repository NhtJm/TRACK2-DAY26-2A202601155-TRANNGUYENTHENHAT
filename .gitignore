__pycache__/
*.pyc
.venv/
.pytest_cache/
runs/

# The world artifact is ~12 MB, belongs to kit/ (which we never submit or
# modify), and README.md says it ships from Releases deliberately uncommitted.
kit/world/*/
!kit/world/*.py

# Tooling state, not part of the submission.
.omc/
